using UnityEngine;
public class EventListItemContentProgressbar : EventListItemContent
{
    // Fields
    protected UnityEngine.UI.Slider slider;
    protected UnityEngine.UI.Text text;
    
    // Methods
    public virtual void SetupSlider(string sliderText, float sliderValue)
    {
        if(this.text == 0)
        {
            
        }
    
    }
    public EventListItemContentProgressbar()
    {
        val_1 = new UnityEngine.MonoBehaviour();
    }

}
